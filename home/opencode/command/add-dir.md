---
description: Add a directory to the current project
---
Add the directory $ARGUMENTS to the current project as a reference. Create it if it doesn't exist. Ensure files in this directory can be referenced using the @filename syntax in opencode.